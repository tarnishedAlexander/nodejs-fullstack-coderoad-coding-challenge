name: Deploy Fullstack App

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build frontend
        run: |
          cd client
          npm ci
          npm run build

      - name: Install backend
        run: |
          cd server
          npm ci

      - name: Deploy to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
<<<<<<< HEAD
          source: |
            client/dist/**
            server/**
            .env
          target: ~/app/
          recursive: true
=======
          source: "client/dist/*,server/*,server/.*,.env*"
          target: "~/app"
          overwrite: true
          strip_components: 0
>>>>>>> bc43472 (fix: some ec2 errors)

      - name: Restart app on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd ~/app
            [ -f .env ] && mv .env server/
            cd server
            npm ci --production
            pm2 restart app || pm2 start src/server.ts --name app --interpreter tsx
            sudo cp -r ../client/dist/* /var/www/html/
